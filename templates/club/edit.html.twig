{% extends 'base.html.twig' %}
{% block title %}Modifier le club{% endblock %}
{% block body %}
<div class="container mt-4" style="max-width: 700px">
    <h1 class="mb-4">✏️ Modifier le club</h1>

    {{ include('club/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="mt-3">
        <a href="{{ path('app_club_index') }}" class="btn btn-secondary me-2">← Retour</a>
        {{ include('club/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}